import { GridsterLayoutItemDirective } from './gridster-layout-item.directive';

describe('GridsterLayoutItemDirective', () => {
  it('should create an instance', () => {
    const directive = new GridsterLayoutItemDirective();
    expect(directive).toBeTruthy();
  });
});
